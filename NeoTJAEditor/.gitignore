# スキン素材(音符・背景・打音など)は配布物にもリポジトリにも入れない。
# exe の隣に置いてもらう前提の外部パックで、著作権の都合で同梱しない。
skin/

# 個人の絶対パス(音源・シミュレータの場所など)が入るのでコミットしない。
settings.json

# 起動確認で吐かせている一時ログ。
app_std*.log

# ビルド成果物 / 仮想環境 / キャッシュ
build/
dist/
.venv/
__pycache__/
*.pyc
